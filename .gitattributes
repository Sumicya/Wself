# Keep binary artifacts binary. Do NOT enable global text=auto renormalization
# here: this repo has historically committed CRLF files, and a `text=auto`
# attribute would rewrite every text file on the next add/commit.
*.png binary
*.jpg binary
*.webp binary
*.gif binary
*.so binary
*.jar binary
*.keystore binary
*.jks binary
